#include <iostream>

using namespace std;

int main() {
    cout << stoi("123") << endl; // Convert string (s) to integer (i)
    cout << to_string(123) << endl; // Convert a number to string
    
    return 0;
}